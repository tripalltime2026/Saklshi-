import {sqliteTable,text,integer,uniqueIndex,index} from 'drizzle-orm/sqlite-core';
export const tables=sqliteTable('dining_tables',{id:integer('id').primaryKey(),name:text('name').notNull(),capacity:integer('capacity').notNull(),x:integer('x').notNull(),y:integer('y').notNull(),active:integer('active').notNull().default(1)});
export const menu=sqliteTable('menu_items',{id:text('id').primaryKey(),name:text('name').notNull(),category:text('category').notNull(),price:integer('price').notNull(),active:integer('active').notNull().default(1)});
export const bookings=sqliteTable('bookings',{id:text('id').primaryKey(),date:text('date').notNull(),start:integer('start').notNull(),end:integer('end').notNull(),tableId:integer('table_id').notNull(),guests:integer('guests').notNull(),firstName:text('first_name').notNull(),lastName:text('last_name').notNull(),phone:text('phone').notNull(),birthDay:integer('birth_day').notNull(),birthMonth:integer('birth_month').notNull(),consent:integer('consent').notNull().default(0),consentAt:text('consent_at'),items:text('items').notNull(),notes:text('notes').notNull().default(''),status:text('status').notNull().default('confirmed'),createdAt:text('created_at').notNull()},t=>[index('bookings_date').on(t.date),index('bookings_phone').on(t.phone)]);
export const slots=sqliteTable('booking_slots',{id:text('id').primaryKey(),bookingId:text('booking_id').notNull().references(()=>bookings.id,{onDelete:'cascade'}),tableId:integer('table_id').notNull(),date:text('date').notNull(),minute:integer('minute').notNull()},t=>[uniqueIndex('unique_table_slot').on(t.tableId,t.date,t.minute)]);
